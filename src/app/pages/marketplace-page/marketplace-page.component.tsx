import { FC } from 'react';
import styles from './profile-page.module.scss';

const MarketplacePage: FC = () => {
  return <div className={styles.wrapper}>Marketplace page</div>;
};

export { MarketplacePage };
